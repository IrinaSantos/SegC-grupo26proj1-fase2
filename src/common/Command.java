package common;

public enum Command {

    CREATE,
    ADD,
    RD,
    EC,
    RT,
    RH,
    OUT,
    OK,
    NOK,
    INFO
}

//apagar os ultimos 3 - preciso corrigir no client handler e onde eles tiverem