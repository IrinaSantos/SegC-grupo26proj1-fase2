package common;

/**
 * Enumera os comandos suportados pelo protocolo cliente-servidor.
 */
public enum Command {

    LOGIN,
    ATTESTATION,
    CREATE,
    ADD,
    RD,
    EC,
    RT,
    RH,
    OUT,
    INFO
}
